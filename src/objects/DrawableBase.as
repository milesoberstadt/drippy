package objects
{
	public class DrawableBase
	{
		/**
		 * Not an actual z-index, it's actually just an identifier in case of deletion.
		 * */
		public var layerIndex:LayerIndex;
		
		public function DrawableBase()
		{
		}
	}
}